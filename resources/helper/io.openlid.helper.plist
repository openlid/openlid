<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>io.openlid.helper</string>

    <!--
        BundleProgram is resolved by launchd relative to the .app bundle
        root. SMAppService uses this when the user enables Open-Lid in
        System Settings → Login Items → Allow in the Background.

        For dev installs (scripts/dev-install-helper.sh), this plist is
        rewritten to use ProgramArguments with an absolute path.
    -->
    <key>BundleProgram</key>
    <string>Contents/MacOS/open-lid-helper</string>

    <key>MachServices</key>
    <dict>
        <key>io.openlid.helper</key>
        <true/>
    </dict>

    <key>AssociatedBundleIdentifiers</key>
    <array>
        <string>io.openlid.app</string>
    </array>
</dict>
</plist>
